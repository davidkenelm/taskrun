{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "taskrun.serviceAccountName" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "taskrun.labels" . | nindent 4 }}
{{- end }}
