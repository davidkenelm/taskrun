---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: taskruns.taskrun.io
spec:
  group: taskrun.io
  names:
    kind: TaskRun
    listKind: TaskRunList
    plural: taskruns
    singular: taskrun
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .spec.schedule
      name: Schedule
      type: string
    - jsonPath: .status.startTime
      name: Last Run
      type: date
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          TaskRun is the Schema for the taskruns API.
          It defines a declarative, typed alternative to raw Kubernetes Jobs and CronJobs.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of TaskRun
            properties:
              auth:
                description: auth defines the authentication context inherited by
                  all authAware steps.
                properties:
                  credentialsFrom:
                    description: credentialsFrom references a Secret containing auth
                      credentials.
                    properties:
                      secretRef:
                        description: secretRef references the Secret containing credentials.
                        properties:
                          certKey:
                            description: certKey is the key within the Secret containing
                              the TLS certificate (for mtls).
                            type: string
                          clientIdKey:
                            description: clientIdKey is the key within the Secret
                              containing the client ID.
                            type: string
                          clientSecretKey:
                            description: clientSecretKey is the key within the Secret
                              containing the client secret.
                            type: string
                          keyKey:
                            description: keyKey is the key within the Secret containing
                              the TLS private key (for mtls).
                            type: string
                          name:
                            description: name is the name of the Secret.
                            type: string
                          passwordKey:
                            description: passwordKey is the key within the Secret
                              containing the password (for basic auth).
                            type: string
                          usernameKey:
                            description: usernameKey is the key within the Secret
                              containing the username (for basic auth).
                            type: string
                        required:
                        - name
                        type: object
                    required:
                    - secretRef
                    type: object
                  tokenEndpoint:
                    description: tokenEndpoint is the OIDC token endpoint URL (required
                      when type is oidc).
                    type: string
                  type:
                    description: type is the authentication mechanism.
                    enum:
                    - oidc
                    - basic
                    - mtls
                    - none
                    type: string
                required:
                - type
                type: object
              concurrencyPolicy:
                default: Forbid
                description: concurrencyPolicy specifies how to treat concurrent executions
                  of a scheduled TaskRun.
                enum:
                - Allow
                - Forbid
                - Replace
                type: string
              onFailure:
                description: onFailure defines the retry and failure handling policy.
                properties:
                  backoff:
                    default: exponential
                    description: backoff is the retry backoff strategy.
                    enum:
                    - linear
                    - exponential
                    type: string
                  retries:
                    default: 0
                    description: retries is the number of times to retry on failure.
                    minimum: 0
                    type: integer
                type: object
              schedule:
                description: |-
                  schedule is a cron expression defining when this TaskRun executes.
                  Omit for one-shot Jobs.
                type: string
              steps:
                description: steps is the ordered list of steps to execute.
                items:
                  description: StepSpec defines a single step within a TaskRun.
                  properties:
                    action:
                      description: action references a StepDefinition or ClusterStepDefinition
                        by name.
                      type: string
                    name:
                      description: name is a unique identifier for this step within
                        the TaskRun.
                      pattern: ^[a-z0-9]([a-z0-9\-]*[a-z0-9])?$
                      type: string
                    outputs:
                      description: outputs lists which step outputs to capture for
                        downstream steps.
                      items:
                        type: string
                      type: array
                    params:
                      additionalProperties:
                        type: string
                      description: params are the input parameters for this step,
                        validated against the StepDefinition schema.
                      type: object
                  required:
                  - action
                  - name
                  type: object
                minItems: 1
                type: array
            required:
            - steps
            type: object
          status:
            description: status defines the observed state of TaskRun
            properties:
              completionTime:
                description: completionTime is when the TaskRun finished executing.
                format: date-time
                type: string
              conditions:
                description: conditions represent the current state of the TaskRun.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              phase:
                description: phase is the current lifecycle phase of the TaskRun.
                enum:
                - Pending
                - Running
                - Succeeded
                - Failed
                type: string
              startTime:
                description: startTime is when the TaskRun began executing.
                format: date-time
                type: string
              steps:
                description: steps contains the status of each individual step.
                items:
                  description: StepStatus describes the observed state of a single
                    step execution.
                  properties:
                    duration:
                      description: duration is the wall-clock time this step took
                        to execute.
                      type: string
                    logs:
                      description: logs contains the last N lines of runner container
                        logs for this step.
                      type: string
                    name:
                      description: name is the step name matching StepSpec.Name.
                      type: string
                    outputs:
                      additionalProperties:
                        type: string
                      description: outputs contains the captured output values from
                        this step.
                      type: object
                    phase:
                      description: phase is the current lifecycle phase of this step.
                      enum:
                      - Pending
                      - Running
                      - Succeeded
                      - Failed
                      - Skipped
                      type: string
                  required:
                  - name
                  - phase
                  type: object
                type: array
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
